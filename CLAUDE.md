# Directives

They are in [`AGENTS.md`](AGENTS.md). **Read it now and follow it.**

One copy rather than two identical files: Claude Code reads this one, Codex and the others read
`AGENTS.md`, and a directive that exists twice is a directive that will disagree with itself the
first time one copy is edited.
