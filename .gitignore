# Dependencies. Without this, the first task to add one would commit a few
# thousand files.
node_modules/

# Whatever a build step produces. There is no build today; an agent that adds
# one should not have to remember this file.
dist/
build/

.DS_Store
.vscode/
.idea/
*.log
coverage/

# The Factory pipeline under .xaedalon/ is versioned; .xaedalon/.gitignore keeps
# its run output out. Add whatever another tool writes to the checkout.
