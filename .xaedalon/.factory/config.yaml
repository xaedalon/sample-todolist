# Factory definitions for this project.
#
# Shared with the repository: ../.gitignore ignores only what runs produce
# (task artifacts, state, trash), so anyone who clones it gets these phases,
# workflows and agents with no separate install step.
#
# Whatever is here takes precedence over ~/.xaedalon/.factory.
kind: factory.scope/v1
scope: project

# plugins:
#   - ./plugins/my-plugin.mjs
